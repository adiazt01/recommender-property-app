/**
 * Constants for pagination functionality in the application.
 */
export const MIN_PAGE = 1

/**
 * The maximum number of pages allowed in the pagination system.
 * This constant is used to limit the number of pages that can be navigated to.
 */
export const MAX_PAGE = 100

/**
 * The default number of items to display per page in paginated views.
 * Used as the standard page size across the application.
 */
export const DEFAULT_PAGE_SIZE = 10